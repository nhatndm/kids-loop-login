import { Theme } from '../interface';

// THEME PROPERTY
import box from './box';
import colors from './colors';

const lightTheme: Theme = {
  name: 'Light',
  box,
  colors,
};

export default lightTheme;
