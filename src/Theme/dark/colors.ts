import { ColorsBase } from '../base';
import { StyleProperty } from '../interface';

const colors: StyleProperty = {
  ...ColorsBase,
  background: ColorsBase.semiStrongBlack,
  elementBackground: ColorsBase.nearlyStrongBlack,
};

export default colors;
