import { Theme } from '../interface';

// THEME PROPERTY
import box from './box';
import colors from './colors';

const darkTheme: Theme = {
  name: 'Dark',
  box,
  colors,
};

export default darkTheme;
