import colors from './colors';
import { StyleProperty } from '../interface';

const text: StyleProperty = {
  primaryColor: colors.naviBlue,
  secondaryColor: colors.grey,
  fontWeight: 400,
};

export default text;
